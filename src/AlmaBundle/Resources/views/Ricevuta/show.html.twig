{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ricevuta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ entity.numero }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ricevuta') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
